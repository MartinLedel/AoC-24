# AoC-24

## AoC using rust and cargo

### To see answers

```bash
#Simply  run
cargo run
```

### Project setup

In `files` folder you will have all the raw input from AoC website.

In `src` the code is located with `main.rs` running the code.

Each day will be named as `dayX.rs` and will feature the solution for the that day.
